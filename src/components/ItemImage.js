const ItemImage = (image) => {
  console.log(image);
  return (
    <div className='collection-item-imageWrapper'>
      {/* <img src={image.src} alt={image.alt} className='collection-item__image' /> */}
    </div>
  );
};

export default ItemImage;
